# NiteRide.FM Infrastructure Architecture

*This file will be auto-generated after the first discovery run.*

Run the discovery manually via GitHub Actions or locally with:

```bash
./scripts/discover.sh
python scripts/generate-docs.py
```
