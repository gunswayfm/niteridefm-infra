# CORS Origin Map for NiteRide.FM
# Auto-included via nginx.conf: include /etc/nginx/conf.d/*.conf;
map $http_origin $cors_origin {
    default "*";
    "~^https?://(.*\.)?niteride\.fm$" $http_origin;
    "~^https?://localhost(:[0-9]+)?$" $http_origin;
}
